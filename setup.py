from setuptools import setup

setup(name='gym-nine-mens-morris',
      version='0.0.1',
      install_requires=['gym']
      )

setup(name='gym-tic-tac-toe',
      version='0.0.1',
      install_requires=['gym']
      )
